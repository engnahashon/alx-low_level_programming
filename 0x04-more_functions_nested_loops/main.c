#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 * 
 * Return: 0 (Success)
 */
int main(void)
{
	void print_numbers(void);
	int mul(int a, int b);
	int _isdigit(int c);
	int _isupper(int c);
	return (0);
}
