#include "main.h"
/**
 * _isupper - Checks for uppercase charaacter
 *
 * Return: 1 if uppercase, 0 if otherwise
 */
int _isupper(int c)
{
	int num;

	if (num >= 65 && num <= 90)
		return (1);
	else
		return (0);
}
