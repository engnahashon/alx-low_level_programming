#include <stdio.h>
#include "main.h"

/**
   * main - check the code
    *
     * Return: Always 0.
      */
int main(void)
{
	    int n;

	        n = get_bit(102, 'k');
		    printf("%d\n", n);
		        n = get_bit(92, 1);
			    printf("%d\n", n);
			        n = get_bit(724, 3);
				    printf("%d\n", n);
				        return (0);
}
